# Placeholder — encrypt with sops after adlerkopf age key is provisioned
# Procedure:
#   1. ssh-to-age < /persist/system/etc/ssh/ssh_host_ed25519_key.pub
#   2. Replace &adlerkopf placeholder in .sops.yaml
#   3. sops updatekeys hosts/adlerkopf/secrets.yaml
#   4. sops encrypt secrets in this file as needed
placeholder: initial-setup
