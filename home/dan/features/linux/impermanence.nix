_: {
  home.persistence."/persist" = {
    directories = [
      # yazi history, bookmarks, tab state
      ".local/share/yazi"
      # Screen recordings (wf-recorder output)
      "Videos"
    ];
  };
}
